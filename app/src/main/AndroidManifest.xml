<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <application
        android:theme="@style/AppTheme"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:allowBackup="true"
        android:resizeableActivity="true"
        android:supportsRtl="true">
        <activity android:name=".SettingsActivity" android:screenOrientation="fullUser" />
        <activity android:name=".ToolsActivity" android:screenOrientation="fullUser" />
        <activity android:name=".SourcesActivity" android:screenOrientation="fullUser" />
        <activity android:name=".ChannelSendActivity" android:screenOrientation="fullUser" />
        <activity android:name=".TelegramConnectActivity" android:screenOrientation="fullUser" />
        <activity android:name=".MediaEditorActivity" android:screenOrientation="fullUser" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="fullUser"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
